# LexGraph QA Checklist

## 그래프

- 현재 파일에서 그래프를 열면 노드와 엣지가 정상 생성된다.
- 노드를 드래그한 뒤 탭을 바꾸고 돌아와도 위치가 유지된다.
- 줌/패닝 후 그래프를 다시 열어도 뷰 상태가 유지된다.
- 사이드바 크기를 바꿔도 그래프가 초기화되지 않고 비례 복원된다.
- 검색창에 노드명을 입력하면 매칭 노드와 1-hop 이웃만 보인다.
- `PNG`, `JSON` 내보내기가 정상 동작한다.

## 법률 기능

- 쟁점 패널에서 항목을 클릭하면 관련 노드가 하이라이트된다.
- 증거 패널에 자동 추출된 증거/사실이 보인다.
- 수동 증거 추가, 편집, 삭제가 동작한다.
- 수동 사실 추가, 편집, 삭제가 동작한다.
- 당사자 관계도와 관계 목록이 정상 표시된다.
- AI 분석 결과가 Markdown으로 렌더링된다.

## Open API

- 설정에 `OC`를 넣으면 법령 조문 조회가 동작한다.
- 설정에 `OC`를 넣으면 판례 조회가 동작한다.
- `OC`가 비어 있으면 관련 버튼이 비활성 또는 안내 상태로 유지된다.

## 편집기

- 법률 엔티티 하이라이트 토글을 켜면 사건번호, 조문, 당사자, 법원이 하이라이트된다.
- 토글을 끄면 에디터 하이라이트가 즉시 제거된다.

## 멀티 문서

- 폴더 분석 시 문서 노드가 생성된다.
- 위키링크가 있으면 문서 간 `위키링크` 엣지가 보인다.
- 공통 키워드가 있으면 문서 간 `공통개념` 엣지가 보인다.

## 영속화

- `그래프 상태 저장`을 켜면 레이아웃과 수동 데이터가 복원된다.
- `그래프 상태 저장`을 끄면 새 세션에서 복원되지 않는다.
- `현재 문서의 저장된 그래프 상태 초기화` 명령이 동작한다.
